-- 0048 — "סוגרים את התשלום בוואטסאפ" (בקשת מרינה, 2026-09).
--
-- לא לכל משפחה יש ביט/פייבוקס מסודר. המוכרת יכולה להדליק אפשרות
-- שבה הקונה מקבלת, אחרי ההזמנה, כפתור שפותח שיחה בוואטסאפ עם הודעה
-- מוכנה על התשלום — והן קובעות ביניהן. המספר של המוכרת לא יושב
-- ב-HTML כרגיל: הוא מגיע מתשובת השרת של יצירת ההזמנה בלבד.

alter table stores add column if not exists payout_whatsapp boolean;

comment on column stores.payout_whatsapp is
  'הקונה יכולה לסגור את התשלום בשיחת וואטסאפ. null/false = כבוי.';
