# שילוב חנות האביזרים בתוך אפליקציית האדרנלין (Marinkayam/Acro)

מסמך זה מרכז כל מה שצריך כדי לבנות את "חנות האביזרים" (קישורי אפיליאציה
לאלי אקספרס) **בתוך** אפליקציית האדרנלין הקיימת, עם העיצוב והמיתוג שלה.

## המטרה

עמוד ייעודי באפליקציה (למשל `/gear` או `/אביזרים`) שמציג את אביזרי
האקרובטיקה כגלריית כרטיסים, עם סינון לפי קטגוריה וחיפוש. כל מוצר מקשר
לאלי אקספרס דרך קישור אפיליאציה.

## המסר המרכזי (חשוב!)

בראש העמוד, באנר בולט וברור:

> 💜 **כל רכישה דרך העמוד הזה מצטברת לקופת התרומות של הנבחרת.**
> הקנייה מתבצעת באלי אקספרס במחיר רגיל — לא עולה לכם שקל נוסף —
> והעמלה שאנחנו מקבלים הולכת ישירות לנבחרת.

זה משמש גם כגילוי נאות (affiliate disclosure) וגם כמוטיבציה להורים.

## הנתונים

- מקור האמת לנתוני המוצרים: **`data/products.json`** בריפו `Marinkayam/store`
  (ענף `claude/acrobatics-landing-page-jug09h`).
- מבנה: `{ categories: [{id,label}], products: [{name,desc,category,emoji,image,link}] }`.
- אפשר להעתיק את ה-JSON ישירות לתוך אפליקציית האדרנלין (למשל
  `src/data/gear.json`), או לייבא אותו.
- עדכון/הוספת מוצרים: עורכים את `tools/products.input.json` ב-`store`
  ומריצים `node tools/aliexpress-sync.mjs` — זה מושך שם/תמונה/מחיר/עמלה
  ולינק אפיליאציה, וכותב מחדש את הנתונים.

## מה לבנות באפליקציית Acro

1. **Route/עמוד חדש** (App Router: `app/gear/page.tsx`, או Pages:
   `pages/gear.tsx`) — לפי המבנה הקיים של הפרויקט.
2. **שימוש ב-Header/Footer/Layout הקיימים** של האדרנלין כדי שהעמוד ייראה חלק.
3. **רכיב גלריה**: גריד כרטיסים responsive, פילטרים לפי קטגוריה (להציג רק
   קטגוריות שיש בהן מוצרים), ושדה חיפוש. כל כרטיס: תמונה/אייקון, שם,
   תיאור, ומחיר אם קיים, וכפתור "לצפייה והזמנה באלי אקספרס".
4. **קישורים**: `target="_blank"` + `rel="noopener sponsored"`.
5. **באנר התרומות** מהסעיף למעלה, בראש העמוד.
6. **קישור בתפריט הראשי** של האתר אל העמוד החדש ("אביזרים" / "ציוד לעונה").

## הנחיות עיצוב

- להשתמש ב-design system / tokens / רכיבי ה-UI הקיימים של האדרנלין
  (צבעים, פונטים, כפתורים) — לא להמציא עיצוב חדש.
- לשמור על RTL ועברית.
- הכרטיסים והפילטרים בעיצוב הנוכחי של החנות העצמאית יכולים לשמש כהשראה
  (`store/index.html`, `store/css/style.css`), אבל הסטיילינג צריך לבוא
  מהאדרנלין.

## איך לפתוח לי גישה לריפו Acro

כדי שאוכל לבצע את השילוב בעצמי, צריך אחת מהשתיים:
1. **להוסיף את `Marinkayam/Acro` לסשן הנוכחי** (בהגדרות הריפוז של הסביבה /
   הסשן ב-Claude Code web), ואז להגיד לי "הוספתי".
2. **לפתוח משימה/סשן חדש על הריפו `Acro`** ולהפנות את Claude למסמך הזה
   (`Marinkayam/store` → `INTEGRATION.md`) ולנתונים ב-`data/products.json`.
